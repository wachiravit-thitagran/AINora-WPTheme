# Dependencies
node_modules/
# Only ignore the Composer vendor/ at the repo root — NOT the self-hosted
# libraries in assets/js/vendor/ (canvas-confetti ships with the theme).
/vendor/

# Build / packaging output
dist/
*.zip

# Logs & caches
npm-debug.log*
*.log
.phpunit.result.cache
.cache/

# Environment / local config
.env
.env.*
*.local

# OS files
.DS_Store
Thumbs.db

# Editor / IDE
.idea/
.vscode/
*.swp
